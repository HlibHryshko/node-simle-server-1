# node-simle-server-1
udemy nodeJs course assignment 1

application that hosts a server on localhost:3000, 
'/'-greeting + submit form for creating a new user,
when the button was pressed, a user name is written in console;
'users'-a list of users;
